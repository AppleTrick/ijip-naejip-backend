<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.home.mapper.UserMapper">

    <resultMap id="userResultMap" type="User">
        <id property="id" column="id" />
        <result property="email" column="email" />
        <result property="password" column="password" />
        <result property="name" column="name" />
        <result property="phone" column="phone" />
        <result property="profileImage" column="profile_image" />
        <result property="role" column="role" />
        <result property="socialType" column="social_type" />
        <result property="socialId" column="social_id" />
        <result property="isEmailVerified" column="is_email_verified" />
        <result property="gender" column="gender" />
        <result property="ageGroup" column="age_group" typeHandler="com.ssafy.home.handler.AgeGroupTypeHandler" />
        <result property="job" column="job" />
        <result property="maritalStatus" column="marital_status" />
        <result property="appPush" column="app_push" />
        <result property="emailNotification" column="email_notification" />
        <result property="marketingNotification" column="marketing_notification" />
        <result property="myHouseName" column="my_house_name" />
        <result property="myHouseAddress" column="my_house_address" />
        <result property="myHouseArea" column="my_house_area" />
        <result property="myHouseFloor" column="my_house_floor" />
        <result property="myHousePrice" column="my_house_price" />
        <result property="createdAt" column="created_at" />
        <result property="updatedAt" column="updated_at" />
        <result property="deletedAt" column="deleted_at" />
    </resultMap>

    <insert id="save" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (
            email, password, name, phone, profile_image, 
            role, social_type, social_id, is_email_verified, 
            gender, age_group, job, marital_status,
            app_push, email_notification, marketing_notification,
            my_house_name, my_house_address, my_house_area, my_house_floor, my_house_price,
            created_at, updated_at
        ) VALUES (
            #{email}, #{password}, #{name}, #{phone}, #{profileImage}, 
            #{role}, #{socialType}, #{socialId}, #{isEmailVerified}, 
            #{gender}, #{ageGroup.value}, #{job}, #{maritalStatus},
            #{appPush}, #{emailNotification}, #{marketingNotification},
            #{myHouseName}, #{myHouseAddress}, #{myHouseArea}, #{myHouseFloor}, #{myHousePrice},
            NOW(), NOW()
        )
    </insert>

    <select id="findByEmail" parameterType="string" resultMap="userResultMap">
        SELECT * FROM users WHERE email = #{email} AND deleted_at IS NULL
    </select>

    <select id="findById" parameterType="long" resultMap="userResultMap">
        SELECT * FROM users WHERE id = #{id} AND deleted_at IS NULL
    </select>

    <select id="existsByEmail" parameterType="string" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE email = #{email} AND deleted_at IS NULL
    </select>

    <select id="findBySocialId" parameterType="map" resultMap="userResultMap">
        SELECT * FROM users WHERE social_type = #{socialType} AND social_id = #{socialId} AND deleted_at IS NULL
    </select>

    <update id="update" parameterType="User">
        UPDATE users
        SET 
            password = #{password},
            name = #{name},
            phone = #{phone},
            profile_image = #{profileImage},
            is_email_verified = #{isEmailVerified},
            gender = #{gender},
            age_group = #{ageGroup.value},
            job = #{job},
            marital_status = #{maritalStatus},
            app_push = #{appPush},
            email_notification = #{emailNotification},
            marketing_notification = #{marketingNotification},
            my_house_name = #{myHouseName},
            my_house_address = #{myHouseAddress},
            my_house_area = #{myHouseArea},
            my_house_floor = #{myHouseFloor},
            my_house_price = #{myHousePrice},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="delete" parameterType="long">
        UPDATE users
        SET deleted_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>
